name: intro2ai
channels:
  - conda-forge
  - defaults
dependencies:
  # Conda owns the interpreter; project dependencies come from pyproject.toml.
  - python=3.10
  - pip>=23
  - setuptools>=68
  - wheel
  - pip:
      - -e .
variables:
  PYTHONNOUSERSITE: "1"
